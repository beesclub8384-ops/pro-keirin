# 작업 지시: 등급별 분석 제거 (데이터 정확성 확보)

## 배경

현재 배당률 분석 페이지(`/analysis/odds`)에서 경주 등급(특선/우수/선발)을 **경주번호로 추측**하고 있다. 이 추측이 부정확할 수 있으므로, **등급 관련 분석을 모두 제거**한다.

### 현재 등급 추론 로직 (삭제 대상)

```
경주번호 10 이상 → 특선급 (부정확할 수 있음)
경주번호 5~9 → 우수급 (부정확할 수 있음)  
경주번호 1~4 → 선발급 (부정확할 수 있음)
```

**이 로직은 기본 규칙일 뿐, 실제로는 경주일마다 편성이 바뀔 수 있어서 틀린 데이터가 됨.**

---

## 작업 내용

### 1단계: 등급 추론 코드 제거

`src/data/odds-data.ts` 파일에서:
- `grade` 필드 추론 로직 삭제 (경주번호 기반 등급 분류)
- `GradeGroup` 타입 삭제 또는 미사용 처리
- `getAverageOddsByGrade()` 등 등급 기반 분석 함수 삭제

### 2단계: 등급 관련 UI 컴포넌트 제거 또는 교체

`src/components/analysis/` 폴더에서:
- `OddsBarChart.tsx` — 등급별 평균 배당 막대그래프 → **삭제 또는 다른 분석으로 교체**
- `OddsSummaryCards.tsx` — 등급 관련 카드 제거
- `AiInsight.tsx` — 등급 언급하는 텍스트 수정

### 3단계: 페이지에서 등급 섹션 제거

`src/app/analysis/odds/page.tsx`에서:
- 등급별 분석 섹션 제거
- 남은 정확한 분석만 표시

### 4단계: 정확한 데이터만으로 재구성

**삭제해야 할 것 (부정확):**
- 등급별 평균 배당률
- 등급별 이변 빈도
- 등급별 어떤 비교든 전부

**유지해야 할 것 (100% 정확):**
- 전체 배당률 분포 (단승/쌍승/삼쌍승 등)
- 경륜장별 분석 (광명/부산/창원)
- 월별/기간별 배당 추이
- 고배당(이변) 빈도 (전체 기준)
- 승식별 평균 배당률
- 매출액 통계

**새로 추가 가능한 분석 (정확한 데이터 기반):**
- 승식별 배당 분포 비교 (단승 vs 쌍승 vs 삼쌍승)
- 경륜장별 평균 배당 비교
- 월별 고배당 출현 빈도
- 요일별 배당 패턴 (데이터에 요일 정보가 있다면)

---

## 주의사항

- 등급 관련 코드/UI를 제거한 후 빌드 에러가 없는지 확인
- 데이터 출처 문구: "data.go.kr 공공 API 기반 2024년 실제 경주결과 3,120건"
- 페이지 하단에 안내 추가: "등급별 분석은 정확한 데이터 확보 후 제공 예정입니다"
- git commit 메시지: "Remove inaccurate grade-based analysis, keep verified data only"

---

## 완료 기준

1. 빌드 성공 (npm run build 에러 없음)
2. 등급(특선/우수/선발) 관련 분석이 페이지에서 완전히 제거됨
3. 남은 분석이 모두 정확한 데이터 기반
4. 페이지가 비어 보이지 않도록 정확한 분석으로 대체됨
